import {useQuery} from '@tanstack/react-query';import {Link} from 'react-router-dom';import {api,list} from '../lib/api';
export default function Home(){const q=useQuery({queryKey:['products'],queryFn:()=>api('/products/'),refetchInterval:60000});return <main><section className="hero"><small>TRUSTED LOCAL COMMERCE</small><h1>Buy confidently.<br/>Sell sustainably.</h1><p>Verified sellers, protected payments, and delivery built for Zambia.</p><Link className="cta" to="/products">Explore products</Link></section><section className="section"><h2>Latest products</h2>{q.isLoading&&<p>Loading…</p>}{q.error&&<p role="alert">{q.error.message}</p>}<div className="grid">{list(q.data).map(p=><Link className="card" to={`/products/${p.id}`} key={p.id}><div className="image">{p.images?.[0]?.image_url?<img src={p.images[0].image_url} alt=""/>:'Optimistic'}</div><small>{p.category_name}</small><h3>{p.name}</h3><p>{p.seller_name}</p><strong>K {p.price}</strong></Link>)}</div></section></main>}
