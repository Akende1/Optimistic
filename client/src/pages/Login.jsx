import{useState}from'react';import{useNavigate,Link}from'react-router-dom';import{api,auth}from'../lib/api';import{Screen}from'../components/State';
export default function Login(){const nav=useNavigate();const[form,setForm]=useState({username:'',password:''});const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{const data=await api('/auth/login/',{method:'POST',body:JSON.stringify(form)});auth.set(data.access,data.refresh,data.user);nav(data.user?.role==='SELLER'?'/seller':data.user?.role==='COURIER'?'/courier':'/');}catch(e){setError(e.message)}finally{setBusy(false)}}return <Screen eyebrow="WELCOME BACK" title="Sign in" description="Access orders, fulfillment, and delivery updates."><form className="form card-panel" onSubmit={submit}><label>Username<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label>{error&&<p className="error" role="alert">{error}</p>}<button disabled={busy}>{busy?'Signing in…':'Sign in'}</button><p>New to Optimistic? <Link className="text-link" to="/register">Create an account</Link></p></form></Screen>}
