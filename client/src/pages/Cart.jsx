import {Link} from'react-router-dom';import {useCart} from '../stores/cart';import{Screen}from'../components/State';export default function Cart(){const{lines,remove}=useCart();const total=lines.reduce((n,x)=>n+Number(x.price)*x.quantity,0);return <Screen eyebrow="YOUR BASKET" title="Cart" description="Review quantities before secure server-priced checkout.">{!lines.length&&<div className="notice">Your cart is empty.</div>}{lines.map(x=><div className="line" key={x.product}><span>{x.name} × {x.quantity}</span><strong>K {(Number(x.price)*x.quantity).toFixed(2)}</strong><button className="button-quiet" onClick={()=>remove(x.product)}>Remove</button></div>)}<div className="cart-total"><div><small>ESTIMATED SUBTOTAL</small><h2>K {total.toFixed(2)}</h2><p>The server confirms the authoritative total and delivery fee.</p></div>{lines.length>0&&<Link className="cta" to="/checkout">Continue to checkout</Link>}</div></Screen>}
