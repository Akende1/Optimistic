import{useQuery,useMutation,useQueryClient}from'@tanstack/react-query';import{api,list}from'../lib/api';import{Screen,Status}from'../components/State';export default function Notifications(){const qc=useQueryClient();const q=useQuery({queryKey:['notifications'],queryFn:()=>api('/notifications/'),refetchInterval:60000});const read=useMutation({mutationFn:id=>api(`/notifications/${id}/read/`,{method:'POST'}),onSuccess:()=>qc.invalidateQueries({queryKey:['notifications']})});const rows=list(q.data);return <Screen eyebrow="UPDATES" title="Notifications" description="Payment, order, delivery, and account messages."><Status loading={q.isLoading} error={q.error} empty={!q.isLoading&&!rows.length}/><div className="stack">{rows.map(n=><button className={`notification ${n.is_read?'read':''}`} key={n.id} onClick={()=>read.mutate(n.id)}><strong>{n.title}</strong><span>{n.message}</span><small>{new Date(n.created_at).toLocaleString('en-ZM')}</small></button>)}</div></Screen>}
